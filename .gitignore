# Raw GitHub API output for PRIVATE repositories, including unfiltered commit
# messages. The published SVGs are redacted; this file is not. It must never be
# committed to a public repository.
tools/data.json

# Local visual-verification pages, regenerated by tools/preview.mjs.
preview-*.html
